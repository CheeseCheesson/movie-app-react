import {getAll, fetchGenres, fetchSession, getRatedMovies, createPostRate} from './service'

const API = {
    getAll,
    fetchGenres,
    fetchSession,
    getRatedMovies,
    createPostRate,
    // getSearchQuery
}

export default API
