import {getAll, fetchGenres, fetchSession, getRatedMovies, createPostRate, getSearchQuery} from './service'

const API = {
    getAll,
    fetchGenres,
    fetchSession,
    getRatedMovies,
    createPostRate,
    getSearchQuery
}

export default API
